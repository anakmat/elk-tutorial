[elasticsearch-{{ _es_combined.version }}]
name=Elasticsearch repository for {{ _es_combined.version }}.x packages
baseurl=http://packages.elasticsearch.org/elasticsearch/{{ _es_combined.version }}/centos
gpgcheck=1
gpgkey={{ elasticsearch_rpm_gpg_key }}
enabled=1
skip_if_unavailable=1
